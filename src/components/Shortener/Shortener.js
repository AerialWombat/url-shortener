import React from "react";

const Shortener = () => (
  <div>
    <h1>/Shortener</h1>
    <input
      type="text"
      placeholder="This is where a URL shortener would be. IF I HAD ONE"
    />
  </div>
);

export default Shortener;
